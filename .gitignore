# Input IFC models (large binaries, local only)
ifcs/

# Ontology files (developed in the FiCR Ontology project; local only,
# updated frequently — not tracked here)
FiCR_ontology/

# FiCR outputs, coverage reports, Protege catalogs and intermediate Stage A
# dumps (all regenerable / session-local)
ficr_outputs/
*_lbd.ttl

# IFCtoLBD JAR (170MB+, download separately)
*.jar

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
